name: deploy-to-github-pages

on:
  release:
    types: [released]
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  publish-pages:
    permissions:
      contents: write
    uses: cable8mm/.github/.github/workflows/deploy-to-github-pages.yml@main
